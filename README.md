# PR #6626: Decision import notice evidence

Synthetic test data only. The PNGs are unmodified browser screenshots.

Before screenshots show the staging custom-agent import panel, restored temporarily for the capture. After screenshots show the PR implementation: missing-model guidance and its Open guide action. The PR also tests the selected-model informational notice.

The images are kept on this separate evidence branch so temporary review assets do not enter the application PR.
